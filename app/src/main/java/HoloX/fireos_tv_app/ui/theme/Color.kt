package HoloX.fireos_tv_app.ui.theme

import androidx.compose.ui.graphics.Color

// Backgrounds (from webapp)
val BgTop = Color(0xFF0F172A)      // #0f172a
val BgBottom = Color(0xFF0B1220)   // deeper navy

// Surfaces
val Surface = Color(0xFF0B1220)
val SurfaceElevated = Color(0xFF111827) // slightly lighter for cards

// Accents
val Accent = Color(0xFF60A5FA)     // #60a5fa (focus/primary)
val Accent2 = Color(0xFF22D3EE)    // close to cyan secondary

// Text
val TextPrimary = Color(0xFFE2E8F0) // light slate
val TextMuted = Color(0xFF94A3B8)   // muted slate
